/** @type {import('next').NextConfig} */
const nextConfig = {
  // `next build` output will be in the `out` folder.
  // see https://nextjs.org/docs/app/building-your-application/deploying/static-exports
  output: 'export',
  reactStrictMode: false,
}

module.exports = nextConfig
